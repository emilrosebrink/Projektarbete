import { Popover } from "radix-ui";
import styled from "styled-components";

//Extended Radix component with styled-components
export const StyledRoot = styled(Popover.Root)`
    background-color: yellowgreen;
`;
